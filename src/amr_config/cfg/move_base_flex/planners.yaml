planners:
  - name: "GlobalPlanner"
    type: "global_planner/GlobalPlanner"
  # - name: "SBPLLatticePlanner"
  #   type: "SBPLLatticePlanner"

planner_patience: 5.0
GlobalPlanner: # Also see: http://wiki.ros.org/global_planner
  # Default
  old_navfn_behavior: false # Exactly mirror behavior of navfn, use defaults for other boolean parameters, default false
  use_quadratic: true # Use the quadratic approximation of the potential. Otherwise, use a simpler calculation, default true
  use_dijkstra: true # Use dijkstra's algorithm. Otherwise, A*, default true
  use_grid_path: False # Create a path that follows the grid boundaries. Otherwise, use a gradient descent method, default false

  # Straigh line
  # use_dijkstra: false
  # use_grid_path: true

  allow_unknown:
    true # Allow planner to plan through unknown space, default true
    # Needs to have track_unknown_space: true in the obstacle / voxel layer (in costmap_commons_param) to work
  planner_window_x: 0.0 # default 0.0
  planner_window_y: 0.0 # default 0.0
  default_tolerance: 0.2 # If goal in obstacle, plan to the closest point in radius default_tolerance, default 0.0

  lethal_cost: 253 # (int, default: 253)
  neutral_cost: 66 # (int, default: 50)
  cost_factor: 0.55 # default 3.0 Kết hợp với neutral_cost và lethal_cost để robot tìm đường tránh vật cản
# SBPLLatticePlanner:
#   planner_type: "ARAPlanner"
#   allocated_time: 10.0 # default: 10.0
#   initial_epsilon: 15.0
#   environment_type: "XYThetaLattice" # The only environment supported
#   forward_search: false # default is false
#   # NOTE: param 'primitive_filename'. This needs to be set to the path to 'ceres_navigation/mprim/ceres_5cm.mprim'
#   # You may put it in the mprim.yaml file, which is ignored by git, so everybody can setup his own path.
#   nominalvel_mpersecs: 0.7
#   timetoturn45degsinplace_secs: 1.8
# # #   lethal_obstacle: 250
